CourtSimulator/
│
├── .env                        # TU CLAVE DE SUPABASE (¡NUNCA subir a GitHub!)
├── .gitignore                  # Archivos que Git debe ignorar
├── requirements.txt            # Dependencias del proyecto
├── pipeline_run.py             # Tu script principal (el que tiene el menú CLI)
├── backfill_checkpoint.json    # (Opcional) Guarda el progreso local
│
├── scraper/                    # Carpeta para todos los módulos de scraping
│   ├── __init__.py             # Archivo vacío, le dice a Python que esto es un paquete
│   ├── results.py
│   ├── match_detail.py
│   ├── player_profile.py
│   └── rounds.py
│
└── .github/
    └── workflows/
        └── daily_scrape.yml    # El "cerebro" que le dirá a la nube cuándo correr